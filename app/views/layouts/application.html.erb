<!DOCTYPE html>
<html>
  <head>
    <title>CrispyBassoon</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if @current_user %>
      <%= form_with url: login_path, method: :delete do |f| %>
        <%= f.submit 'Sign out' %>
      <% end %>
    <% end %>
    <%= yield %>
  </body>
</html>
